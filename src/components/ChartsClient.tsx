"use client";

import React from 'react';

// Charts were removed from the project. This stub prevents import errors if any
// leftover references exist. It renders nothing.
export default function ChartsClient(_: any) {
  return null;
}
